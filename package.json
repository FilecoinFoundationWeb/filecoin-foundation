{
  "name": "filecoin-foundation",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@10.9.0",
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
