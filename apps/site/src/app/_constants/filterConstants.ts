export const DEFAULT_FILTER_ID = 'all'

export const DEFAULT_CATEGORY_FILTER_OPTION = {
  id: DEFAULT_FILTER_ID,
  name: 'All Categories',
} as const

export const DEFAULT_LOCATION_FILTER_OPTION = {
  id: DEFAULT_FILTER_ID,
  name: 'All Locations',
} as const
